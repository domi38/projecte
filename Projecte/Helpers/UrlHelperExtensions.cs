using System;
using System.Web.Mvc;

namespace Echk.Web.Helpers
{
    public static class UrlHelperExtensions
    {


        /// <summary>
        /// By now only works the RoutedActions.ALL... weird problems with the switch and the enums flag
        /// </summary>
        /// <param name="helper"></param>
        /// <param name="actions"></param>
        /// <returns></returns>
        //public static Object Routes(this UrlHelper helper, RoutedActions actions)
        //{
        //    return Routes(helper, actions, null);
        //}

        /// <summary>
        /// By now only works the RoutedActions.ALL... weird problems with the switch and the enums flag
        /// </summary>
        /// <param name="helper"></param>
        /// <param name="actions"></param>
        /// <returns></returns>
        //public static Object Routes(this UrlHelper helper, RoutedActions actions, IEnumerable<long?> ids)

    }
}